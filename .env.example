# clickOR operational defaults (optional).
#
# Copy this to .env and edit. The CLI loads .env automatically (unless --no-env).
#
# Local mode (running on the same machine that has the ErsatzTV sqlite DB):
#   CLICKOR_DB_PATH=/path/to/ersatztv.sqlite3
#
# Remote mode (DB lives on a server/NUC you can SSH into):
#   CLICKOR_SSH=ssh -i /full/path/to/key user@host
#   CLICKOR_DB_PATH=/mnt/media/config/ersatztv.sqlite3
#   CLICKOR_SSH_SUDO=1
#
# If you want clickor apply to reset playout after writing the DB:
#   CLICKOR_BASE_URL=http://<host>:8409
#   CLICKOR_RESET_AFTER_APPLY=1

CLICKOR_SSH=
CLICKOR_DB_PATH=
CLICKOR_SSH_SUDO=1
CLICKOR_BASE_URL=
CLICKOR_RESET_AFTER_APPLY=1

